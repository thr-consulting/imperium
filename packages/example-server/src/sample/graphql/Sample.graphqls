extend type Query {
	getCounter: Int
	getCounterSecured: Int
	getLocalDate: LocalDate
	getLocalTime: LocalTime
	getMoney: Money
	getJwt: String
}

extend type Mutation {
	incCounter: Int
	setLocalDate(date: LocalDate): Boolean
	setLocalTime(time: LocalTime): Boolean
	setMoney(amount: Money): Boolean
}

extend type Subscription {
	counterChanged: Int
}
