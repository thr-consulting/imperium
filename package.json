{
	"author": "Mike Kornelson <darkadept@durbn.net>",
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "https://github.com/darkadept/imperium.git"
	},
	"private": true,
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"lerna": "lerna",
		"build": "yarn build:auth && yarn build:graphql && yarn build:noauth",
		"build:auth": "cd packages/auth && yarn build",
		"build:noauth": "cd packages/noauth && yarn build",
		"build:graphql": "cd packages/graphql && yarn build",
		"lint": "lerna run lint",
		"test": "lerna run test --stream --concurrency 1",
		"test:coverage": "lerna run test:coverage --stream --concurrency 1",
		"pkghash": "./tools/pkghash",
		"docs:md": "lerna run docs:md",
		"docs:build": "yarn docs:md && cd website && yarn build",
		"ready:commit": "yarn lint && yarn test:coverage && yarn docs:build"
	},
	"devDependencies": {
		"@babel/cli": "^7.1.2",
		"@babel/core": "^7.1.2",
		"concurrently": "^4.0.1",
		"eslint": "^5.9.0",
		"express": "^4.16.3",
		"immutable": "^3.8.2",
		"lerna": "^3.4.0",
		"mongodb": "^3.1.6",
		"react": "^16.5.2",
		"react-dom": "^16.5.2",
		"react-router-dom": "^4.3.1",
		"semantic-ui-react": "^0.84.0"
	}
}
